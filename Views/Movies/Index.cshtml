@model IEnumerable<CinemaSharpAuth.Models.Movie>

@{
    ViewBag.Title = "Movies Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Movies</h2>
@if(Model.Any())
{
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th>Genre</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var movie in Model)
            {
                <tr class="table-dark">
                    <th scope="row"><a href=/movies/details/@movie.Id>@movie.Name</a></th>
                    <th>@movie.Genre.Name</th>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No movies available at the current moment.</p>
}
